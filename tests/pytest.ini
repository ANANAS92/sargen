[pytest]
;see additional options at https://pypi.org/project/pytest-xdist/
;addopts = -n auto ; for threads auto detection
addopts = --cov=sargen.sources --cov-report html --cov-fail-under=80 -n auto
